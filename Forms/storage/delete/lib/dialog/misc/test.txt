
        #self.layout = QVBoxLayout()
        #
        ## Πίνακας δεδομένων
        #self.tableView = QTableView(self)
        #self.layout.addWidget(self.tableView)

        ## Διάταξη για τα κουμπιά
        #self.button_layout = QHBoxLayout()
        #
        ## Κουμπιά
        #self.btn_edit = QPushButton("Επεξεργασία")
        #self.btn_delete = QPushButton("Διαγραφή")
        #
        #self.button_layout.addWidget(self.btn_edit)
        #self.button_layout.addWidget(self.btn_delete)
        #
        #self.layout.addLayout(self.button_layout)  # Προσθήκη των κουμπιών στο κύριο layout
        #self.setLayout(self.layout)
#
        ## Φόρτωση δεδομένων
        #self.load_data()
#
        ## Συνδέσεις κουμπιών
        #self.btn_edit.clicked.connect(self.edit_selected)
        #self.btn_delete.clicked.connect(self.delete_selected)

    #def load_data(self):
    #    """Φορτώνει τα δεδομένα στον πίνακα"""
    #    query_model = QSqlQueryModel()
    #    query = QSqlQuery("SELECT * FROM product")  # Χρησιμοποιούμε το κατάλληλο SQL query
    #    query_model.setQuery(query)
    #    
    #    if query_model.lastError().isValid():
    #        print(f"Query Error: {query_model.lastError().text()}")
    #    
    #    self.tableView.setModel(query_model)
#
    #def edit_selected(self):
    #    """Επεξεργάζεται το επιλεγμένο στοιχείο"""
    #    selected = self.tableView.selectionModel().selectedRows()
    #    if not selected:
    #        QMessageBox.warning(self, "Σφάλμα", "Δεν επιλέξατε εγγραφή για επεξεργασία!")
    #        return
    #    
    #    row = selected[0].row()
    #    dialog = QDialog(self)
    #    dialog.setWindowTitle("Επεξεργασία Στοιχείου")
    #    dialog.setGeometry(200, 200, 400, 300)
    #    layout = QVBoxLayout()
    #    dialog.setLayout(layout)
    #    
    #    QMessageBox.information(self, "Επεξεργασία", "Ανοίγει παράθυρο επεξεργασίας (placeholder)")
    #    
    #    if dialog.exec():
    #        self.load_data()
    #
    #def delete_selected(self):
    #    """Διαγράφει το επιλεγμένο στοιχείο"""
    #    selected = self.tableView.selectionModel().selectedRows()
    #    if not selected:
    #        QMessageBox.warning(self, "Σφάλμα", "Δεν επιλέξατε εγγραφή για διαγραφή!")
    #        return
    #    
    #    for index in selected:
    #        self.model.removeRow(index.row())
#
    #    self.model.submitAll()
    #    self.load_data()  # Επαναφόρτωση δεδομένων
